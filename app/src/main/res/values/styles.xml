<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- this theme is use in activity manifest   Theme.AppCompat.Light.NoActionBar-->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:listSelector">@color/accent</item>
        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicator</item>
        <!--<item name="android:textAppearance">@color/black_primary_text</item>-->
        <!--<item name="android:dialogTheme">@style/spinner_style</item>-->
    </style>

    <style name="toolBarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white_primary_text</item>
        <item name="android:textColorSecondary">@color/white_secondary_text</item>
        <item name="android:actionMenuTextColor">@color/white_primary_text</item>
        <item name="android:textColorPrimaryInverse">@color/white_primary_text</item>
    </style>

    <style name="CustomTabPageIndicator">
        <item name="android:gravity">center</item>
        <item name="android:background">@color/accent</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAppearance">@style/TextAppearance.TabPageIndicator</item>
        <item name="android:textSize">15sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
    <!-- Style for forecast listview -->
    <style name="ListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
        for two-pane layout -->
    </style>
</resources>
